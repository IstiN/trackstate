# Plan

1. Resolve the workflow merge conflict and stage it.
2. Enable cache-busting for hosted `session` reads so `projectConfig` reflects the latest save.
3. Remove the unrelated `.dmtools/config.js` churn from the PR.
4. Run the focused test coverage, then the full test suite.
