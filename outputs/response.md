All open review comments remain addressed, and the branch-sync failure with `origin/main` was resolved directly in the merged tree.
Merge/sync fix: resolved the live conflict in `lib/ui/features/tracker/views/trackstate_app.dart` by taking the merged `origin/main` connected-control palette behavior, then aligned the checked-out `trackstate-setup` submodule to the merged fixture revision so the on-disk setup template matches the newer tombstones-based repository index expected by the merged tests.
Earlier TS-116 fixes remain in place: `ProviderBackedTrackStateRepository` no longer uses the stale nullable `_session?.resolvedUserIdentity` path, the shared settings test robot still handles both boolean and tri-state `isFocused` semantics, and the Copilot/cloud-agent Flutter setup fix remains in the branch.
Validation: PASS — `flutter analyze`, `flutter test --coverage` (53 tests), the targeted setup fixture regression (`flutter test test/trackstate_repository_test.dart --plain-name 'checked-in setup template includes repository index artifacts and richer fixtures'`), plus the earlier TS-87 Python regression suite.
