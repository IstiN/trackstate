test_id: TS-464
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Seeds a Local Git repository fixture with only the en locale configured.
  - Verifies Settings > Locales exposes a validated locale-code selector for adding fr and immediately scaffolds config/i18n/fr.json plus project.json supportedLocales without any extra hidden setup.
