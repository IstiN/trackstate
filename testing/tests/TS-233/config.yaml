test_id: TS-233
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the Dashboard top-bar Create issue flow in Local Git mode blocks submission when Summary is blank.
  - Confirms a visible summary-required validation error is shown and the form stays open with Description preserved.
  - Confirms Local Git storage remains unchanged (no new issue keys, file, commit, or worktree changes).
