test_id: TS-145
type: cli
framework: unittest
platform: python
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or via TS132_FLUTTER_BIN / TS115_FLUTTER_BIN / TRACKSTATE_FLUTTER_BIN
notes:
  - Runs the live enforcement command `dart run tool/check_theme_tokens.dart ./lib` against the repository root.
  - Passes only when the dot-relative production UI path resolves successfully, shows the success message, and emits zero analyzer-style warnings.
