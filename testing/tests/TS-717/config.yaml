test_id: TS-717
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Verifies a committed local TrackState repository is inspected as ready-to-open.
  - Verifies the onboarding UI moves to Workspace details with the Open workspace action enabled, uses the directory picker callback, keeps interactive semantics labels non-empty, and leaves the selected folder unchanged on disk.
runtime_inputs:
  run_command: flutter test testing/tests/TS-717/test_ts_717.dart --reporter expanded
  expected_inspection_state: readyToOpen
  expected_status_label: Ready to open
  expected_action_label: Open workspace
