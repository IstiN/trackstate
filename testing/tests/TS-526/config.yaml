test_id: TS-526
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Uses the deployed hosted TrackState app against the live setup repository.
  - Temporarily switches the live DEMO project to repository-path attachment storage when needed, then restores the original project.json after the run.
  - Expects the Attachments tab to hide browser upload controls and show the repository-path restriction notice instead.
