test_id: TS-100
type: api
framework: unittest
platform: python
dependencies:
  - TS-95 live delegate fix deployed on main
  - shared Python Dart runtime bootstrap in testing/frameworks/python/dart_probe_runtime.py
  - offline dart pub get in testing/tests/TS-100/dart_probe
runtime_bootstrap:
  - cached Dart SDK root defaults to ~/.cache/trackstate-test-tools and can be overridden with TS38_TOOL_CACHE or TRACKSTATE_TOOL_CACHE
  - Dart SDK archive/checksum metadata lives in testing/core/config/dart_sdk_runtime.json
notes:
  - Verifies one previously captured ProviderBackedTrackStateRepository.session object stays live across connecting -> connected -> disconnected -> connected.
  - Confirms identity and capability flags mutate on the same session reference instead of requiring callers to re-read repository.session.
