test_id: TS-139
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Reproduces the dirty Local Git create-entry visibility scenario from the live TrackState app.
  - Verifies Dashboard, Board, JQL Search, Hierarchy, and Settings each expose a visible, reachable Create issue entry point while the repository has uncommitted changes.
  - Confirms the reachable create flow can still surface commit/stash/clean recovery guidance after submit in dirty state.
