test_id: TS-725
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with one active hosted workspace and one inactive hosted workspace preloaded in browser storage.
  - Verifies the active hosted row upgrades after sign-in while the inactive hosted row keeps the deterministic non-live Needs sign-in state before and after signing in from the active hosted workspace.
