test_id: TS-194
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-66 deleteIssue tombstone persistence coverage
  - TS-192 delete flow keeps legacy deleted index untouched
notes:
  - Exercises LocalTrackStateRepository.deleteIssue against a real local Git-backed fixture whose TRACK-122 directory contains both main.md and attachment.txt.
  - Verifies the delete removes the issue from observable search results and physically removes the TRACK/TRACK-122 directory instead of leaving an empty orphaned folder behind.
