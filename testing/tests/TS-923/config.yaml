test_id: TS-923
type: cli
framework: python
platform: flutter-analyze
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or via TS923_FLUTTER_BIN / TRACKSTATE_FLUTTER_BIN
notes:
  - Copies the repository to a disposable temp workspace so the production checkout stays unchanged.
  - Verifies the sync attention-needed semantic label getter is still used by the tracker app and that the English localization value keeps the required `Sync error` prefix.
  - Runs `flutter analyze lib/ui/features/tracker/views/trackstate_app.dart` and only passes when the live implementation remains analyzer-clean with `No issues found!`.
