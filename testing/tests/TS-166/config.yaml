test_id: TS-166
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-161 archiveIssue implementation deployed on main
  - TS-135 archive lifecycle persistence fixture coverage
notes:
  - Reuses the LocalTrackStateRepository archive fixture to verify persistence across repository restart.
  - Confirms the archived state is visible immediately after archiving and remains visible after a fresh repository instance reloads the same data directory.
