test_id: TS-189
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-141]
notes:
  - Reuses the TS-141 Local Git custom-fields fixture so the production-visible JQL Search create flow renders Solution, Acceptance Criteria, and Diagrams.
  - Verifies the Create issue entry point is visible in the JQL Search top bar, opens the form from that section specifically, and persists both core and custom fields to DEMO/DEMO-2/main.md.
