test_id: TS-495
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Temporarily switches the live DEMO project attachment storage mode to github-releases with a ticket-specific release tag prefix.
  - Opens the deployed hosted app in a browser-authenticated session and inspects Project Settings > Repository access.
  - Passes only when the overall repository access band stays connected, the GitHub Releases attachment callout uses success styling, the supported browser-upload copy is visible, and the blanket local-Git warning is absent.
