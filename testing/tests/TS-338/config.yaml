test_id: TS-338
type: cli
framework: unittest
platform: python
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH
notes:
  - Executes the exact ticket commands `trackstate search --jql "project = TRACK" --startAt "first"` and `trackstate search --jql "project = TRACK" --maxResults -10` from a seeded temporary TrackState repository.
  - Compiles a temporary CLI binary from this checkout so the seeded repository can remain the current working directory.
  - Uses a supported local-search control command against the same seeded repository to prove the fixture itself is valid before asserting the validation errors.
