test_id: TS-211
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-174 archive existing issue coverage
  - TS-205 archive index stale-path fix deployed on main
notes:
  - Reuses the TS-174 Local Git archive fixture and the shared IssueKeyResolutionService.
  - Verifies archiveIssue updates same-session key resolution from the active issue path to the archive storage path.
  - Confirms repository-consumer search still finds TRACK-122 and surfaces the archived storage path after archiving.
