test_id: TS-525
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Seeds a live hosted DEMO issue with a repository-path attachment for deterministic download coverage.
  - Uses a Playwright GitHub API route override that patches the repository permissions response to read-only so canUpload resolves to false in the hosted session.
  - Verifies the deployed Attachments tab keeps download access while hiding upload and replacement controls from both visible UI and keyboard traversal.
