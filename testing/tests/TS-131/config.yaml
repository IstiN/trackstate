test_id: TS-131
type: ci
framework: unittest
platform: python
dependencies:
  - gh CLI authenticated with read access to IstiN/trackstate and GitHub Actions
  - network access to GitHub REST APIs
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or bootstrapped automatically
runtime_inputs:
  repository: IstiN/trackstate
  workflow_path: .github/workflows/unit-tests.yml
  workflow_name: Flutter Required Checks
  workflow_job_name: Flutter checks
  workflow_step_name: Enforce theme tokens
  gate_command: dart run tool/check_theme_tokens.dart
  recent_run_limit: 10
notes:
  - Validates that the deployed pull-request workflow still exposes the theme-token gate.
  - Reuses the TS-115 hardcoded-hex probe to confirm the gate fails on non-tokenized colors.
