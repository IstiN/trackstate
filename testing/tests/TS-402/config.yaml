test_id: TS-402
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Opens a real Local Git issue, launches the production Edit issue dialog, and verifies keyboard focus order, visible semantics labels, summary-required validation feedback, and empty-summary placeholder contrast.
  - Uses the reusable issue-edit accessibility fixture under testing/ to keep the test layered through components instead of framework calls in the ticket test.
