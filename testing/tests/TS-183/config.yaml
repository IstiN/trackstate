test_id: TS-183
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the Board view in Local Git mode exposes a reachable Create issue top-bar action.
  - Confirms saving from the Board-origin create dialog closes the form, creates the issue, and makes it searchable from JQL Search.
  - Confirms the Local Git storage layer persists the new issue as a clean single-file commit.
