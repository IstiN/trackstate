test_id: TS-217
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-170 archiveIssue existing-issue path deployed on main
  - TS-179 archiveIssue relocates directory-level artifacts to archive storage
notes:
  - Reuses the archive-success fixture with a seeded TRACK-122 issue directory that contains main.md plus a sibling artifact.
  - Verifies archiveIssue succeeds, marks the issue archived, relocates every physical issue artifact into archive storage, and removes the active-path copies.
