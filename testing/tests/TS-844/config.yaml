test_id: TS-844
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token.
  - Uses real keyboard navigation to reach the desktop workspace switcher trigger.
  - Opens the workspace switcher with Space, verifies the trigger keeps focus while open with aria-expanded=true, then presses Space again to verify the surface closes with aria-expanded=false.
