test_id: TS-986
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored GitHub token and a preloaded mismatched local workspace as the active startup target plus one hosted fallback workspace.
  - Uses the ticket viewport of 1440x900 and waits for startup hydration to settle into the interactive shell before opening Workspace switcher.
  - Confirms the mismatched local workspace transitions to Unavailable and does not remain selected as the persisted Active / Local Git row.
