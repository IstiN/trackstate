# TS-259

Guards the checked-out `trackstate-setup` submodule, its tracked gitlink commit,
and the live `IstiN/trackstate-setup` Actionlint workflow against using the
unresolved `rhysd/actionlint@v1` reference that breaks setup before linting
begins.
