test_id: TS-232
type: ui
framework: flutter_test
platform: linux
dependencies:
  - TS-141
notes:
  - Reuses the shared Local Git TrackState widget-test flow with a ticket-specific fixture that declares Solution, Acceptance Criteria, and Diagrams in DEMO/config/fields.json.
  - Wraps a preloaded Local Git repository with a deterministic initial load delay so the production loading state is observable before ProjectConfig-backed create fields are available.
  - Verifies the Create issue entry point is unavailable while configuration is still loading, then confirms the loaded form renders the configured custom fields instead of a hardcoded fallback-only form.
