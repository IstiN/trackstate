test_id: TS-766
type: ui
framework: flutter_test
platform: widget
dependencies: []
notes:
  - Uses an isolated mutable hosted repository fixture to emit an attachments-only sync signal for Issue-C.
  - Verifies the visible Attachments tab updates while load_snapshot_delta remains unchanged and no detail/meta hydration is dispatched.
