test_id: TS-937
type: cli
framework: python
platform: flutter-test
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or via TS937_FLUTTER_BIN / TRACKSTATE_FLUTTER_BIN
notes:
  - Verifies the dedicated sync widget contract test exists in the live repository and targets the semanticLabel API with a raw String regression mutation.
  - Confirms the live _SyncPill production signature, typed helper, and call site still require the _SyncPillSemanticLabel wrapper rather than a primitive String contract.
  - Runs `flutter test test/workspace_sync_semantic_label_contract_test.dart -r expanded` and only passes when the dedicated regression test succeeds against the live implementation.
