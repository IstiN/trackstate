# TS-73

## Install dependencies

```bash
flutter pub get
```

## Run this test

```bash
flutter test testing/tests/TS-73/test_ts_73.dart -r expanded
```

## Required configuration

This test reads the checked-in `trackstate-setup/` repository content directly
from the workspace and does not require external credentials or environment
variables.

## Expected passing output

```text
00:00 +1: All tests passed!
```
