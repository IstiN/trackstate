# TS-1402 — Setup-repo integrated smoke path

Runs the complete MVP workflow against the configured setup repository:
session probe, issue creation, workflow transition, JQL search, and cleanup.
Verifies that each step succeeds end-to-end.
