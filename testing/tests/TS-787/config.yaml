test_id: TS-787
type: ui
framework: flutter_test
platform: widget
dependencies:
  - TS-786 empty or unknown hosted sync path filtering deployed on main
notes:
  - Uses a mutable hosted ProviderBackedTrackStateRepository fixture to drive the real TrackState app through the production workspace sync listener.
  - Verifies an empty-string hosted sync changed path is ignored without dispatching issue refreshes or triggering a global snapshot reload.
  - Confirms the visible Comments tab stays unchanged for the user after the malformed sync event is processed.
