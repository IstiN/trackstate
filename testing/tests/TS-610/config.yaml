test_id: TS-610
type: cli
framework: python
platform: local-git
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH
  - GH_TOKEN or GITHUB_TOKEN with release upload access to the hosted setup repository
notes:
  - Seeds a disposable local TrackState repository configured for github-releases attachment storage and pointed at the live setup repository as origin.
  - Pre-creates a GitHub Release containing logic.drawio and a matching local attachments.json entry for TS-123.
  - Executes the exact local CLI command `trackstate attachment upload --issue TS-123 --file logic.drawio --target local`.
  - Captures the GitHub API request order made by the live CLI flow and passes only when it resolves the release tag first, deletes the colliding asset second, and uploads the new bytes third.
  - Verifies the final user-visible state still converges to one replacement logic.drawio asset with updated bytes.
runtime_inputs:
  ticket_command: trackstate attachment upload --issue TS-123 --file logic.drawio --target local
  requested_command:
    - trackstate
    - attachment
    - upload
    - --issue
    - TS-123
    - --file
    - logic.drawio
    - --target
    - local
  project_key: TS
  project_name: TS-610 Project
  issue_key: TS-123
  issue_summary: TS-610 request-order replacement fixture
  source_file_name: logic.drawio
  source_file_text: |-
    <mxfile host="app.diagrams.net" version="24.7.17">
      <diagram id="replacement" name="Page-1">
        <mxGraphModel dx="1000" dy="600" grid="1">
          <root>
            <mxCell id="0"/>
            <mxCell id="1" parent="0"/>
            <mxCell id="2" value="updated" style="rounded=1;" vertex="1" parent="1">
              <mxGeometry x="120" y="120" width="160" height="60" as="geometry"/>
            </mxCell>
          </root>
        </mxGraphModel>
      </diagram>
    </mxfile>
  expected_attachment_name: logic.drawio
  release_tag_prefix_base: ts610-assets-
  seeded_attachment_text: |-
    <mxfile host="app.diagrams.net" version="24.7.17">
      <diagram id="seeded" name="Page-1">
        <mxGraphModel dx="1000" dy="600" grid="1">
          <root>
            <mxCell id="0"/>
            <mxCell id="1" parent="0"/>
            <mxCell id="2" value="seeded" style="rounded=1;" vertex="1" parent="1">
              <mxGeometry x="80" y="80" width="140" height="50" as="geometry"/>
            </mxCell>
          </root>
        </mxGraphModel>
      </diagram>
    </mxfile>
  seeded_attachment_created_at: "2026-05-13T07:00:00Z"
  attachment_media_type: application/xml
  manifest_poll_timeout_seconds: 120
  manifest_poll_interval_seconds: 4
  release_poll_timeout_seconds: 120
  release_poll_interval_seconds: 4
