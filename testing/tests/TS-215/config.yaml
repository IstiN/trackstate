test_id: TS-215
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-136 deleteIssue leaves legacy deleted index untouched on main
  - TS-206 delete flow exclusion of legacy deleted index deployed on main
notes:
  - Uses a ticket-specific Local Git fixture with three active delete targets, one surviving issue, and a seeded legacy deleted.json file.
  - Triggers concurrent LocalTrackStateRepository.deleteIssue calls through the repository service and verifies tombstone artifacts, integrated search results, and legacy deleted index integrity after the concurrent persistence cycle.
