test_id: TS-135
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-79 lifecycle persistence patterns deployed on main
notes:
  - Verifies the repository service archive lifecycle contract through LocalTrackStateRepository.
  - Confirms repository metadata and standard repository search expose the archived state for TRACK-555.
