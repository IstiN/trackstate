test_id: TS-655
type: api
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
  - git CLI available on PATH
notes:
  - Seeds a disposable Local Git-backed repository with source issue DEMO-2 and target issue DEMO-10.
  - Calls the live Local Git storage write API directly for DEMO/DEMO-1/DEMO-2/links.json.
  - Persists a canonical `{"type":"blocks","target":"DEMO-10","direction":"outward"}` record.
  - Verifies the write succeeds, the links.json file matches the requested payload, the reloaded issue exposes the link, and the repository HEAD advances.
