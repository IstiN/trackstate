test_id: TS-229
type: api
framework: unittest
platform: python
dependencies:
  - network access to the public GitHub REST API
  - network access to the public GitHub web pages for releases and tags
runtime_inputs:
  repository: IstiN/trackstate-setup
  expected_stable_version: v1.0.0
