test_id: TS-199
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies that intentionally closing the Local Git Create issue form clears the persisted Summary draft before the next create attempt.
  - Covers the user-visible Cancel path from Dashboard and confirms the reopened form starts with an empty Summary field.
