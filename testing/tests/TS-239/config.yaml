test_id: TS-239
type: ui
framework: flutter_test
platform: linux
dependencies:
  - TS-231
notes:
  - Reuses the shared Local Git TrackState widget-test flow with a ticket-specific fixture that removes the entire DEMO/config directory from the repository.
  - Waits for the production-visible startup state before asserting that Local Git remains usable and Create issue falls back to the core Summary and Description fields.
  - Verifies visible user-facing controls instead of internal repository state only.
