test_id: TS-1261
type: web
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Launches the production first-launch onboarding screen with empty SharedPreferences at the default desktop widget-test viewport of 1440x900.
  - Verifies the private SettingsTextField wrapper source wires label and helper text styling through the muted theme token, then confirms the visible Repository Path label, local helper text, and Branch label render with TrackStateColors.light.muted (#5B5A52) on white.
  - Runs `dart tool/check_theme_tokens.dart lib/ui/features/tracker/views/trackstate_app.dart` so the regression stays covered by the repository theme-token policy gate.
