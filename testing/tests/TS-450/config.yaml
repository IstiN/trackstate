test_id: TS-450
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Uses a hosted provider-backed repository fixture seeded with .trackstate/index/issues.json summary metadata and delayed initial search completion.
  - Verifies that Dashboard keeps bootstrap-backed metric counts and summary tiles visible while unresolved fields stay in a loading state instead of collapsing to a blank surface.
