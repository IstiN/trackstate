test_id: TS-234
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies cancelling top-bar issue creation closes the overlay in Local Git mode.
  - Reopens the create flow and confirms the Summary field is reset (draft discarded).
  - Fails with top-bar, visible-text, and semantics snapshots when controls are missing or stale state persists.
