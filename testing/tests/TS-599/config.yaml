test_id: TS-599
type: api
framework: flutter_test
platform: linux
dependencies:
  - AC - Workflow and resolution enforcement deployed on main
notes:
  - Exercises the concrete IssueMutationService.transitionIssue flow against a temporary Local Git-backed repository whose workflow allows To Do -> In Progress but not To Do -> Done.
  - Verifies the typed validation failure, unchanged persisted main.md frontmatter, unchanged Git revision, and repository search output that integrated clients depend on.
