test_id: TS-126
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-101 arbitrary frontmatter key preservation fix deployed on main
notes:
  - Verifies explicit customFields entries and arbitrary top-level frontmatter keys are merged into one canonical customFields map.
  - Reuses the shared IssueResolutionService and TrackState app screen abstractions.
  - Confirms both repository resolution and the real issue-detail flow remain usable.
