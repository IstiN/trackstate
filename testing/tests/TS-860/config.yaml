test_id: TS-860
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app on Dashboard in a desktop viewport.
  - Locates the visible workspace switcher trigger before any user interaction.
  - Verifies the trigger exposes aria-controls on initial page load and that the referenced DOM element already exists.
