test_id: TS-115
type: cli
framework: unittest
platform: python
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or bootstrapped automatically
notes:
  - Creates a temporary copy of the repository and injects a probe widget under lib/.
  - Verifies a theme-token color expression passes the repository policy gate before swapping in a hardcoded hex color.
  - Expects dart run tool/check_theme_tokens.dart to report a diagnostic that blocks the hardcoded hex regression.
