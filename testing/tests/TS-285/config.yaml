test_id: TS-285
type: api
framework: flutter_test
platform: linux
dependencies:
  - AC - Typed mutation result contract for optimistic concurrency failures
notes:
  - Exercises the concrete IssueMutationService.updateFields path against a temporary Local Git-backed repository.
  - Reproduces a stale expected revision by injecting a real concurrent commit between the service read and the provider revision check, rather than mocking the mutation result.
  - Verifies the conflict is surfaced through the typed result contract and that the current revision is exposed in a machine-readable field.
