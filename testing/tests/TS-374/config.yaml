test_id: TS-374
type: ui
framework: flutter_test
platform: widget
dependencies: []
notes:
  - Uses the shared hosted read-only provider fixture to exercise the production auth-gate UI in a connected but non-writable session.
  - Verifies localized semantics, keyboard reachability, focused CTA treatment, and inline Comments callout contrast on the live widget surface.
