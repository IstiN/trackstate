test_id: TS-153
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-148 archiveIssue lifecycle contract deployed on main
notes:
  - Verifies archiving a non-existent issue returns a repository-domain not-found error instead of a low-level interface crash.
  - Confirms the failed archive leaves repository metadata, issue search results, and the Git worktree unchanged.
