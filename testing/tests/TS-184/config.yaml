test_id: TS-184
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-169 archiveIssue provider failure mapping deployed on main
  - TS-170 archiveIssue existing-issue path deployed on main
notes:
  - Exercises LocalTrackStateRepository.archiveIssue against a real local Git-backed fixture with an existing TRACK-122 issue.
  - Verifies the repository service completes without throwing and exposes TRACK-122 as archived through repository state and search results after the operation.
