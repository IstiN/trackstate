test_id: TS-1360
type: install-script
framework: unittest
platform: linux
dependencies:
  - bash
  - curl
  - Python 3.12+
  - PyYAML
notes:
  - Verifies that the POSIX install script aborts with a clear error when the downloaded archive's SHA256 checksum does not match the published checksum file.
runtime_inputs:
  install_script_path: scripts/install/install.sh
  platform: linux-x64
  binary_name: trackstate
