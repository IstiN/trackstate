test_id: TS-74
type: api
framework: unittest
platform: python
dependencies:
  - GitHub CLI authenticated with a token that can read the setup repository
  - A fork of IstiN/trackstate-setup for the authenticated user or a
    TS74_SETUP_REPOSITORY override that points to such a fork
