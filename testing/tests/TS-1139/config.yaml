test_id: TS-1139
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Reuses the Local Git issue hierarchy fixture from TS-303 to validate Sub-task parent enforcement.
  - Verifies that Create issue blocks submission and shows the visible parent-required validation when Parent is empty.
