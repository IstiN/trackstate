test_id: TS-111
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-41, TS-93]
notes:
  - Reuses the Local Git dirty-save widget flow from the deployed TrackState app.
  - Verifies the visible Save failed banner includes actionable commit/stash/clean guidance.
  - Confirms the banner exposes a functional dismiss control that removes the notification from the UI.
