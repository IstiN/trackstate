test_id: TS-93
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-41]
notes:
  - Verifies the dirty local save error appears promptly in Local Git mode.
  - Attempts the required user-visible dismissal before continuing.
  - Confirms the UI still accepts further navigation after the failed save attempt.
