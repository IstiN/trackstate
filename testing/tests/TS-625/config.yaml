test_id: TS-625
type: api
framework: flutter_test
platform: linux
dependencies:
  - AC - Failure results use stable machine-readable categories
notes:
  - Reuses the existing stale revision conflict fixture from TS-285 to exercise the concrete IssueMutationService.updateFields path against a temporary Local Git-backed repository.
  - Injects a real concurrent commit between the read and write so the mutation executes with an outdated expected revision instead of mocking the failure result.
  - Verifies the returned typed mutation result reports ok=false semantics through isSuccess=false and uses the machine-readable conflict category that integrated clients depend on.
