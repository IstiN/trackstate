test_id: TS-401
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Exercises the hosted tracker at https://istin.github.io/trackstate-setup/ using the stored GitHub token flow.
  - Prefers the live DEMO-5 issue and falls back to another non-epic issue that is not already Done/Highest so reruns still prove a fresh mutation.
  - Uses the current Board edit flow and, when needed, visible setup transitions to stage the issue until the final Done transition is available.
