test_id: TS-401
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Exercises the hosted tracker at https://istin.github.io/trackstate-setup/ using the stored GitHub token flow.
  - Selects a Board-visible non-epic issue that is not already Done/Highest so reruns still prove a fresh mutation.
  - Prefers DEMO-3994 through DEMO-3997 when they are visible on the live Board.
  - Uses the current Board edit flow and falls back to JQL Search opening if the Board card click path is stale; when needed, visible setup transitions stage the issue until the final Done transition is available.
