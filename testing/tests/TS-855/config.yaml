test_id: TS-855
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Reuses the dual-local-workspace production fixture pattern from TS-841.
  - Seeds two saved local workspaces with the second workspace selected before launch.
  - Uses the ticket preconditioned state directly and sends Arrow Up without an extra row click.
  - Verifies the previous workspace becomes active, receives focus, and primaryFocus no longer remains on the desktop switcher container.
runtime_inputs:
  workspace_count: 2
  first_workspace_kind: local
  second_workspace_kind: local
  initial_active_workspace: second
  keyboard_action: Arrow Up
  expected_result: previous workspace becomes active, receives focus, and switcher remains open
  unexpected_focus_after_action: desktop-workspace-switcher
  run_command: flutter test testing/tests/TS-855/test_ts_855.dart --reporter expanded
