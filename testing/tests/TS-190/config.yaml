test_id: TS-190
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-122, TS-141]
notes:
  - Reuses the Local Git create-issue widget-test flow that already validates production-visible Create issue behavior in the deployed app.
  - Configures Solution and Answer custom fields, then verifies YAML-sensitive characters are preserved exactly in the generated DEMO/DEMO-2/main.md frontmatter.
  - Requires the saved customFields frontmatter payload to stay decodable and to preserve both the quoted colon-containing value and the leading-dash value without corruption.
