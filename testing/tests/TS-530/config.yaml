test_id: TS-530
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Uses the deployed hosted TrackState app against the live setup repository.
  - Temporarily switches the live DEMO project to repository-path attachment storage when needed, then restores the original project.json after the run.
  - Verifies the hosted Attachments tab shows the repository-path restriction notice, hides browser upload controls, keeps existing downloads visible below the notice, and routes Open settings to Project Settings > Attachments.
