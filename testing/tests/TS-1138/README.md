Validates that `trackstate read issue-link-types` is a strict alias of
`trackstate read link-types`, returning the same canonical Jira-style link type
payload from the same seeded Local Git repository.
