test_id: TS-162
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-159 repository not-found contract fix deployed on main for archiveIssue
notes:
  - Exercises the missing-issue update flow through the concrete repository edit API `updateIssueDescription`, because the production repository contract exposes dedicated update operations rather than a generic `updateIssue` method.
  - Verifies a missing issue update returns a repository-domain not-found error instead of leaking the low-level Git provider failure, and confirms the repository state remains unchanged.
