test_id: TS-222
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Starts in hosted mode, switches to Local Git with a temporary repository fixture, then attempts the production-visible reverse switch back to hosted mode.
  - Verifies the Dashboard top bar returns to hosted mode and that Settings shows the hosted repository-access status instead of the Local Git fields.
  - Fails with top-bar, visible-text, and semantics snapshots when the runtime does not expose the reverse switch or hosted repository-access surface.
