test_id: TS-445
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Pumps the production Flutter tracker app into the hosted startup-recovery shell state, opens Settings, and verifies the recovery callout in the hosted-access area.
  - Confirms the visible recovery actions, amber warning token usage, WCAG AA text contrast, keyboard focus reachability, hover/focus state styling, and production-visible behavior when the user triggers Retry and Connect GitHub.
