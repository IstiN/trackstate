test_id: TS-209
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-141]
notes:
  - Reuses the TS-141 Local Git custom-fields fixture so the production-visible Create issue form includes Solution, Acceptance Criteria, and Diagrams.
  - Verifies the user can fill the form, cancel it, and observe no filesystem or git-history changes in the Local Git repository.
