test_id: TS-701
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Launches TrackState with a fresh SharedPreferences-backed workspace profile store so no saved or migrated workspaces exist.
  - Verifies the first-launch onboarding route shows Local folder and Hosted repository as equal first-class choices.
runtime_inputs:
  run_command: flutter test testing/tests/TS-701/test_ts_701.dart --reporter expanded
