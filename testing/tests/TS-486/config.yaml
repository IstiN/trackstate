test_id: TS-486
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Forces the viewer locale to de before launching the production Local Git app.
  - Reuses the shared locale-resolution repository fixture together with TrackStateAppScreen and SettingsScreenRobot.
  - Saves the de translation for in-progress as WIP, verifies the repository catalog persists the edit, and confirms the value remains visible after navigating away and back to Settings > Locales.
