test_id: TS-127
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-101 arbitrary top-level frontmatter preservation fix deployed on main
notes:
  - Verifies integer, boolean, and list top-level frontmatter values remain typed inside customFields.
  - Reuses the shared IssueResolutionService and TrackState app screen abstractions.
  - Confirms the real issue-detail flow stays usable with mixed YAML data types.
