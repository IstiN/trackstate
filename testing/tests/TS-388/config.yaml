test_id: TS-388
type: web
framework: playwright
platform: chromium
dependencies:
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed hosted tracker against https://istin.github.io/trackstate-setup/.
  - Uses the live DEMO-2 issue detail to verify visible comment chronology and attachment recency ordering.
  - Records the exact user-visible attachment failure if the tab cannot render a sortable attachment list.
