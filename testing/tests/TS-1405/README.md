# TS-1405 — CI Smoke Reporting

Verifies that the setup-repo smoke automation produces a machine-readable
summary containing the Pages load time and CLI benchmark percentiles, so that
CI reporting can surface these metrics.
