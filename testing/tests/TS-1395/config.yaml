test_id: TS-1395
type: static
framework: unittest
platform: python
dependencies:
  - Python 3.12+
notes:
  - Verifies the repository README mirrors the release quick-start install commands and links to the setup repository documentation instead of duplicating onboarding.
