# TS-262

Validates that a disposable pull request touching only `README.md` in
`IstiN/trackstate-setup` does not surface a failing `actionlint` gate and
remains mergeable from a contributor's point of view when no unrelated
required checks are still gating the branch.
