test_id: TS-1140
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Exercises the live production mutation feedback path by triggering a move validation failure inside TrackStateApp.
  - Verifies the visible error text, the matching Semantics widget label, and the liveRegion semantics configuration required for screen-reader announcements.
