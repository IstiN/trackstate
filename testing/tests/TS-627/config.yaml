test_id: TS-627
type: api
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
  - git CLI available on PATH
notes:
  - Seeds a disposable Local Git-backed repository with a source issue and target issue.
  - Calls the live Local Git storage provider directly to attempt persisting a non-canonical `blocks` + `inward` record into the source issue `links.json`.
  - Verifies the storage-layer response is a validation rejection and that reloading the repository still exposes no persisted link or extra commit.
