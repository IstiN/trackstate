test_id: TS-447
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Exercises the production Flutter tracker shell in the hosted startup-recovery state.
  - Verifies GitHub authentication triggers one automatic bootstrap resume, does not auto-retry after a second 403, and requires an explicit Retry action to resume again.
