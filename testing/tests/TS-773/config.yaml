test_id: TS-773
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Launches the production TrackStateApp with a mutable in-memory repository that implements WorkspaceSyncRepository.
  - Seeds two Open issues in JQL Search, selects TRACK-773-B, then queues a background sync event representing an explicit load_snapshot_delta=1 global refresh request.
  - Verifies the production app-resume sync path calls loadSnapshot exactly once for the queued refresh, updates the selected issue detail from the reloaded snapshot, and reports load_snapshot_delta=1.
