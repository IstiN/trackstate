test_id: TS-773
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Launches the production TrackStateApp with a mutable in-memory repository that implements WorkspaceSyncRepository.
  - Seeds two Open issues in JQL Search, selects TRACK-773-B, then runs both an unflagged hosted sync control and a flagged hosted sync attempt.
  - Passes only when the unflagged hosted sync leaves the global snapshot reload counter unchanged and the explicit attempt exposes a distinct hosted reload signal that triggers exactly one full snapshot reload.
