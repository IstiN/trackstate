test_id: TS-502
type: api
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Exercises the production GitHub release-backed attachment upload flow through ProviderBackedTrackStateRepository and GitHubTrackStateProvider.
  - Verifies a missing release for an existing tag is auto-repaired by creating a new draft release that targets the exact issue tag before uploading the asset and persisting attachments.json.
