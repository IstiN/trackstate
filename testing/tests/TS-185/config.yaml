test_id: TS-185
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-169 archive lock-file sanitization deployed on main
notes:
  - Exercises LocalTrackStateRepository.archiveIssue against a real local Git-backed fixture with an existing TRACK-122 issue.
  - Creates a real .git/index.lock file before archiving so the production Git CLI path fails without a mocked process runner.
