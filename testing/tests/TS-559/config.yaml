test_id: TS-559
type: cli
framework: python
platform: local-git
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH
  - gh CLI available on PATH
  - GH_TOKEN or GITHUB_TOKEN with release upload access to the hosted setup repository
notes:
  - Seeds a disposable local TrackState repository configured for github-releases attachment storage with a valid GitHub origin.
  - Executes the exact local CLI command `trackstate attachment upload --issue TS-999 --file setup.log --target local`.
  - Verifies the command succeeds, `attachments.json` records the release-backed metadata, and `gh release view ts559-assets-TS-999` exposes a draft release titled `Attachments for TS-999` containing the uploaded asset.
runtime_inputs:
  ticket_command: trackstate attachment upload --issue TS-999 --file setup.log --target local
  requested_command:
    - trackstate
    - attachment
    - upload
    - --issue
    - TS-999
    - --file
    - setup.log
    - --target
    - local
  project_key: TS
  project_name: TS-559 Project
  issue_key: TS-999
  issue_summary: TS-559 draft release creation fixture
  source_file_name: setup.log
  source_file_text: |-
    TS-559 upload payload
    Verifies the local github-releases upload path creates a missing machine-managed draft release container.
  release_tag_prefix_base: ts559-assets-
  expected_sanitized_asset_name: setup.log
  manifest_poll_timeout_seconds: 120
  manifest_poll_interval_seconds: 4
  release_poll_timeout_seconds: 120
  release_poll_interval_seconds: 4
