test_id: TS-830
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token.
  - Verifies sequential desktop keyboard navigation reaches the workspace switcher trigger in the live desktop shell sequence without skipping it.
  - Verifies the focused trigger shows a visible keyboard focus indicator.
