test_id: TS-964
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored GitHub token and a preloaded broken local workspace as the active startup target plus one hosted fallback workspace.
  - Uses the ticket viewport of 1440x900 and verifies startup does not hard-stop on the Sync issue surface.
  - Confirms the application shell, header workspace switcher trigger, and open switcher surface remain usable while the broken local workspace is present as an unavailable recovery row.
