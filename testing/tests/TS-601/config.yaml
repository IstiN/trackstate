test_id: TS-601
type: api
framework: flutter_test
platform: linux
dependencies:
  - Production IssueMutationService.archiveIssue running against LocalTrackStateRepository on main
notes:
  - Exercises archiving EPIC-10 through the shared mutation service while TASK-20 remains an active child issue.
  - Verifies the archive succeeds, the parent moves into archive storage, and the child remains visible in active repository index/search results.
