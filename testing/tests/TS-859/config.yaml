test_id: TS-859
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token and deterministic saved workspace profiles.
  - Uses real keyboard navigation to focus the workspace switcher trigger and open the surface with Space.
  - Tabs to the in-panel Repository field, presses Space, and verifies the trigger toggle logic does not dismiss the open surface.
