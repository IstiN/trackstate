test_id: TS-922
type: cli
framework: python
platform: flutter-analyze
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or via TS922_FLUTTER_BIN / TRACKSTATE_FLUTTER_BIN
notes:
  - Copies the repository to a disposable temp workspace so the production checkout stays unchanged.
  - Replaces one sync widget semanticLabel argument with the raw string `Attention needed`.
  - Runs `flutter analyze lib/ui/features/tracker/views/trackstate_app.dart` and only passes when the mutation is blocked by a real type or argument diagnostic.
