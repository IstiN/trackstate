test_id: TS-545
type: cli
framework: python
platform: local-git
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH
  - gh CLI available on PATH
  - GH_TOKEN or GITHUB_TOKEN with release upload access to the hosted setup repository
notes:
  - Seeds a disposable local TrackState repository configured for github-releases attachment storage and pointed at the live setup repository as origin.
  - Executes the exact local CLI command `trackstate attachment upload --issue TS-100 --file test-upload.txt --target local`.
  - Verifies the command succeeds without a repository capability-gate failure and that the uploaded asset is visible in the live GitHub Release via `gh release view`.
runtime_inputs:
  ticket_command: trackstate attachment upload --issue TS-100 --file test-upload.txt --target local
  requested_command:
    - trackstate
    - attachment
    - upload
    - --issue
    - TS-100
    - --file
    - test-upload.txt
    - --target
    - local
  project_key: TS
  project_name: TS-545 Project
  issue_key: TS-100
  issue_summary: TS-545 local github-releases capability-gate fixture
  source_file_name: test-upload.txt
  source_file_text: |-
    TS-545 upload payload
    Verifies the local-git github-releases capability gate permits real release-backed uploads.
  release_tag_prefix_base: ts545-assets-
  expected_sanitized_asset_name: test-upload.txt
  manifest_poll_timeout_seconds: 120
  manifest_poll_interval_seconds: 4
  release_poll_timeout_seconds: 120
  release_poll_interval_seconds: 4
