test_id: TS-400
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Uses a Local Git repository fixture with Epic, Story, and Sub-task issue types enabled.
  - Verifies the production Edit issue dialog keeps Epic read-only for sub-tasks and re-derives Epic when Parent changes from Story-A to Story-B.
