test_id: TS-146
type: cli
framework: unittest
platform: python
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or via TS132_FLUTTER_BIN / TS115_FLUTTER_BIN / TRACKSTATE_FLUTTER_BIN
notes:
  - Reuses the shared ThemeTokenPolicyDirectoryValidator to run `dart run tool/check_theme_tokens.dart ./invalid_path_xyz`.
  - Verifies `./invalid_path_xyz` is absent before execution so the ticket still covers the intended missing-target scenario.
  - Passes only when the command exits with status 2, prints the missing-target diagnostic, and omits the success banner.
