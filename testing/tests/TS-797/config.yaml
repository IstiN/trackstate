test_id: TS-797
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token.
  - Verifies the already-open workspace switcher morphs from the desktop anchored panel to a mobile bottom sheet and back again when the viewport changes.
