test_id: TS-621
type: api
framework: flutter_test
platform: linux
dependencies:
  - AC - Workflow transition validation deployed on main
notes:
  - Exercises the shared IssueMutationService.transitionIssue flow against a temporary Local Git-backed repository whose workflow allows To Do -> In Progress but not To Do -> Done.
  - Verifies the returned validation failure, unchanged persisted issue markdown and Git revision, and the unchanged repository search result a user or integrated client would observe.
