test_id: TS-649
type: cli
framework: unittest
platform: python
dependencies:
  - Flutter SDK available on PATH
  - git CLI available on PATH
notes:
  - Reuses the existing disposable Local Git CLI link harness from TS-629.
  - Creates Issue A and Issue B, then runs `trackstate ticket link --type "relates to"` from Issue A to Issue B.
  - Verifies the user-visible JSON response preserves the symmetric link label and returns canonical outward metadata anchored to the source issue.
