test_id: TS-1137
type: web
framework: playwright
platform: chromium
dependencies:
  - GH_TOKEN or GITHUB_TOKEN configured for the hosted repository
notes:
  - Drives the live Settings workspace on the deployed TrackState app.
  - Clicks Save settings without changing Statuses or Workflows and expects a visible failure explaining that no Git commit was produced.
