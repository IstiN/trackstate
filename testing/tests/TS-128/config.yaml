test_id: TS-128
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-101 arbitrary frontmatter preservation fix deployed on main
notes:
  - Verifies arbitrary top-level frontmatter keys with empty and explicit null values are preserved in customFields.
  - Reuses the shared IssueResolutionService and TrackState app screen abstractions.
  - Confirms both the resolved canonical model and the real issue-detail flow stay usable.
