test_id: TS-150
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-122, TS-141]
notes:
  - Reuses the shared Local Git TrackState widget-test flow and drives it with a ticket-specific repository fixture.
  - Declares a unique custom field named Verification Notes in DEMO/config/fields.json before opening the production-visible Create issue form.
  - Passes only when the form renders Verification Notes as a visible input, accepts the entered value, creates the issue successfully, and persists that value in DEMO/DEMO-2/main.md.
