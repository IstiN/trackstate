test_id: TS-195
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-174 archiveIssue existing-issue path deployed on main
  - TS-179 archiveIssue relocates directory-level artifacts to archive storage
notes:
  - Exercises LocalTrackStateRepository.archiveIssue against a real local Git-backed fixture whose issue directory contains both main.md and attachment.txt.
  - Verifies active storage is cleared and the archive storage path contains the relocated markdown and attachment artifacts.
