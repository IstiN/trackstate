test_id: TS-875
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token and two preloaded saved hosted workspaces.
  - Opens the desktop workspace switcher from Dashboard and focuses the visible Save and switch button inside the panel.
  - Presses Arrow Down from that nested button and verifies the active saved workspace advances while the panel remains open.
