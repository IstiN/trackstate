test_id: TS-681
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with browser storage preloaded with one hosted workspace profile that omits legacy metadata fields.
  - Verifies startup still reaches the interactive shell and Saved workspaces renders safe fallback defaults for the partial hosted entry.
