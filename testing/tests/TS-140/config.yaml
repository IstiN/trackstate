test_id: TS-140
type: ui
framework: flutter_test
platform: linux
dependencies:
  - TS-94
  - TS-123
notes:
  - Starts issue creation while the Local Git repository is dirty.
  - Verifies visible commit/stash/clean recovery guidance on the first submit attempt.
  - Stashes the filesystem changes outside the app and retries the same create form.
  - Expects the dialog to close and the new issue to be committed on retry.
