test_id: TS-303
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Uses a Local Git repository fixture with Epic, Story, and Sub-task issue types enabled.
  - Verifies Epic hides hierarchy fields, Sub-task requires Parent, and Epic is derived read-only from the selected Parent.
