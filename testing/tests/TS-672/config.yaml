test_id: TS-672
type: api
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Seeds the production SharedPreferencesWorkspaceProfileService with local workspaces for `/user/projects/app` on `main` and `/user/projects/temp` on `main`.
  - Verifies updating the `/user/projects/temp` workspace to `/user/projects/app` on `main` is rejected as a duplicate.
  - Verifies the observable saved-workspace state remains unchanged after the rejected update.
