test_id: TS-465
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Seeds a Local Git repository fixture with en as the default locale and de as a second configured locale.
  - Verifies Settings > Locales disables removing the current default locale, allows removing a non-default locale after switching the default to de, and still blocks removing the last remaining locale.
  - Uses visible locale chips, the Default locale dropdown, and the Remove locale action state for user-facing verification.
