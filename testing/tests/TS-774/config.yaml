test_id: TS-774
type: ui
framework: flutter_test
platform: widget
dependencies: []
notes:
  - Reuses the mutable hosted ProviderBackedTrackStateRepository fixture from TS-734.
  - Verifies a project metadata sync preserves the visible Dashboard counters for unchanged issue data, refreshes Settings Attachments data, and does not increment the hosted snapshot reload counter.
