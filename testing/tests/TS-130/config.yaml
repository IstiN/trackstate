test_id: TS-130
type: cli
framework: unittest
platform: python
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or bootstrapped automatically
notes:
  - Creates a temporary copy of the repository and injects lib/ts115_lint_probe.dart.
  - Verifies dart run tool/check_theme_tokens.dart accepts a tokenized color before rejecting Color(0xFFFAF8F4).
  - Confirms the terminal diagnostic includes the hardcoded literal and probe file path a user would rely on.
