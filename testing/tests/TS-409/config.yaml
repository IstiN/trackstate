test_id: TS-409
type: web
framework: playwright
platform: chromium
dependencies:
  - GH_TOKEN or GITHUB_TOKEN configured for the hosted repository
notes:
  - Drives the live Settings Admin workspace on the deployed TrackState app.
  - Verifies one hosted Git commit is created for the saved settings change and that `trackstate session` exposes the updated projectConfig data immediately afterward.
