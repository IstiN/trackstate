test_id: TS-1358
type: install-script
framework: unittest
platform: linux
dependencies:
  - bash
  - curl
  - Python 3.12+
  - PyYAML
notes:
  - Verifies the POSIX install script resolves the latest release, downloads the platform archive, verifies its checksum, installs the binary locally, and appends the install directory to the shell profile without requiring sudo.
runtime_inputs:
  install_script_path: scripts/install/install.sh
  platform: linux-x64
  binary_name: trackstate
  install_dir_relative: .trackstate/bin
