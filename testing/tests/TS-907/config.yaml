test_id: TS-907
type: cli
framework: python
platform: flutter-analyze
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or via TS907_FLUTTER_BIN / TRACKSTATE_FLUTTER_BIN
notes:
  - Copies the repository to a disposable temp workspace so the production checkout stays unchanged.
  - Replaces `_workspaceSyncAttentionNeededSemanticLabel(l10n)` with the weaker `_workspaceSyncAttentionNeededVisibleLabel(l10n)` helper call in the temp copy.
  - Runs `flutter analyze lib/ui/features/tracker/views/trackstate_app.dart` and only passes when the local analysis command surfaces a real diagnostic instead of a clean `No issues found!` result.
