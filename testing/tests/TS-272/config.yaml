test_id: TS-272
type: cli
framework: unittest
platform: python
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH for seeding a disposable Local Git repository
notes:
  - Executes `trackstate session --target local --path <temp-repo>` without `--output`.
  - Verifies stdout is valid JSON and exposes the versioned TrackState success envelope keys.
