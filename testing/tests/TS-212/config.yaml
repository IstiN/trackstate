test_id: TS-212
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-174 archiveIssue existing-issue path deployed on main
  - TS-205 archiveIssue creates archive storage and updates archived path on main
notes:
  - Exercises LocalTrackStateRepository.archiveIssue against a real local Git-backed fixture whose repository starts without TRACK/.trackstate/archive/.
  - Verifies the missing archive directory is created, TRACK-122 is relocated into it, and repository consumers resolve the archived storage path after the move.
