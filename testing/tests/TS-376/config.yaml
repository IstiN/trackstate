test_id: TS-376
type: cli
framework: unittest
platform: python
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH
notes:
  - Compiles a temporary `trackstate` executable from this checkout.
  - Executes the exact ticket command `trackstate read issue-link-types` from a seeded Local Git repository.
  - Verifies the CLI returns the four canonical Jira-style issue link types with the expected inward and outward labels.
