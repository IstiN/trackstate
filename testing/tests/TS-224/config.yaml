test_id: TS-224
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-208, TS-207]
notes:
  - Reuses the malformed fields.json Local Git fallback setup and waits for the visible parse error before asserting save behavior.
  - Verifies the production-visible Create issue fallback path saves Summary and Description successfully while fields.json remains invalid.
  - Confirms the new Local Git issue is visible through search and detail UI and is persisted as a dedicated Git commit with a clean worktree.
