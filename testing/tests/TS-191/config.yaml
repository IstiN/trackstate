test_id: TS-191
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-141]
notes:
  - Reuses the TS-141 Local Git custom-fields fixture so the production-visible Create issue form renders Summary, Description, Solution, and Acceptance Criteria.
  - Verifies valid custom-field input does not bypass the required Summary validation, keeps the form open with the entered values visible, and leaves the Local Git repository unchanged.
