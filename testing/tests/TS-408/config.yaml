test_id: TS-408
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Exercises the hosted Settings > Fields administration flow on https://istin.github.io/trackstate-setup/ using the stored GitHub token flow.
  - Verifies the reserved Summary field remains protected and captures a live bug reproduction if the deployed editor exposes mutable controls.
