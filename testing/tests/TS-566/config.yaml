test_id: TS-566
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Uses the deployed hosted TrackState app against the live setup repository.
  - Temporarily switches the live DEMO project to github-releases storage with a deterministic tag prefix, then restores the original project.json after the run.
  - Applies the linked TS-520 production fix semantics: in hosted browser sessions, GitHub Releases uploads must be blocked with the CORS explanation and hidden upload controls rather than attempting a browser upload.
