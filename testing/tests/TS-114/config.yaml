test_id: TS-114
type: web
framework: flutter_test
platform: flutter-widget
dependencies: [TS-52]
notes:
  - Reuses the Settings screen robot and DemoTrackStateRepository to surface the visible Connected status in Settings.
  - Verifies the Connected container background matches the centralized success token instead of the legacy hardcoded value.
