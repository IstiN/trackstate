test_id: TS-888
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
notes:
  - Opens the deployed TrackState hosted app in a fresh Playwright Chromium context with a stored GitHub token.
  - Verifies the primary navigation exposes Settings, transitions into Project Settings, and allows the user to switch among core admin tabs.
