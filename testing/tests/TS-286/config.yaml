test_id: TS-286
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Drives the production `TrackStateApp` board move flow with a ticket-scoped repository that forces a validation failure.
  - Verifies the failure is visible as user-facing text and exposed through an exact semantics label.
  - Verifies the failure notification is announced as a live-region style screen-reader alert instead of relying on color only.
