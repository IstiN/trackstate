test_id: TS-122
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the Create issue entry point is visible from Dashboard, Board, JQL Search, and Hierarchy in Local Git mode.
  - Confirms each visible entry point is user-reachable by opening the create flow and checking the rendered Summary, Description, Save, and Cancel controls.
  - Fails with per-section visible text and semantics snapshots when the entry point is missing or not reachable.
