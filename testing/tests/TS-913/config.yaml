test_id: TS-913
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored signed-in GitHub token.
  - Preloads an active local workspace plus one hosted workspace in browser storage.
  - Establishes the Local Unavailable precondition by deleting the prepared local repository before startup and waiting for retry exhaustion.
  - Restores the same local repository on disk outside the app, refreshes the page, and verifies the workspace stays Local Unavailable until a manual action occurs.
