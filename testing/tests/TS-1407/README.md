# TS-1407 — Setup repository validation

Validates that the setup template repository contains the required sample data
and configuration files, and that the structure matches the candidate storage
model. The test fails when expected template data is missing or out of sync.
