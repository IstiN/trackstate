test_id: TS-469
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Seeds a Local Git repository fixture with English and French locale metadata, including deliberate missing French translations that should surface fallback warnings in Settings > Locales.
  - Reuses the shared SettingsScreenRobot to verify visible content, semantics labels, warning treatment tokens and contrast, placeholder contrast, and keyboard focus order through the translation matrix.
