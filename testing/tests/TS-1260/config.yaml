test_id: TS-1260
type: web
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Launches the production first-launch onboarding screen in a WidgetTester harness with empty saved workspace state.
  - Verifies the local onboarding flow keeps the visible `Repository Path` label, `Enter the local Git folder path.` helper text, and `Branch` label on-screen with AA-compliant contrast.
  - Asserts the live rendered text uses the deployed muted token outcome `#5B5A52` on `#FFFFFF`, which renders at `6.93:1`.
