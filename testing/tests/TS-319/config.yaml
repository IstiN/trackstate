test_id: TS-319
type: cli
framework: unittest
platform: python
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH
notes:
  - Executes the exact ticket command `trackstate search --jql "project = TRACK" --startAt 0 --maxResults 2` from a seeded temporary TrackState repository.
  - Compiles a temporary CLI binary from this checkout so the seeded repository can remain the current working directory.
  - Runs a supported local-search control command against the same seeded repository to confirm the fixture itself is valid.
