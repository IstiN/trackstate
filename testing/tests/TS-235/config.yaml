test_id: TS-235
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies Local Git mode keeps a visible top-bar Create issue entry point in Dashboard, Board, JQL Search, and Hierarchy.
  - Confirms the Create issue control exposes an accessibility-facing Create issue semantics label in each section.
  - Fails with section-specific top-bar text and semantics snapshots to aid debugging.
