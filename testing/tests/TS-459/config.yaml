test_id: TS-459
type: cli
framework: unittest
platform: python
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH
notes:
  - Executes the Jira-compatible hierarchy alias commands from a seeded Local Git repository using a compiled binary from this checkout.
  - Verifies a single `--parent` input resolves to `epic` for story creation under an Epic and to `parent` for sub-task reassignment under a Story.
  - Confirms both commands produce the expected canonical repository paths and user-visible CLI JSON envelopes.
