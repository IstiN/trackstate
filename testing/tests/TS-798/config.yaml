test_id: TS-798
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token.
  - Verifies the desktop workspace switcher opens without modal-style background dimming.
  - Verifies the visible desktop header remains interactive while the switcher is open.
