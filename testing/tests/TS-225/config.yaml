test_id: TS-225
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-216]
notes:
  - Reuses the shared Local Git TrackState widget-test flow with a ticket-specific malformed fields.json fixture.
  - Waits for the malformed config path to surface the parse error before asserting the visible Local Git runtime state.
  - Verifies the user-facing repository access chrome stays in Local Git mode and does not regress to an AI-only provider surface.
