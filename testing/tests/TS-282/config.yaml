test_id: TS-282
type: api
framework: flutter_test
platform: linux
dependencies:
  - Local Git-backed repository writes enabled through the production LocalTrackStateRepository provider
notes:
  - Exercises the concrete IssueMutationService.reassignIssue flow against a temporary repository where TASK-1 starts under EPIC-1.
  - Verifies the typed success result, the Git-visible directory move to EPIC-2/TASK-1, the rebuilt repository index, and user-facing issue lookup after the move.
