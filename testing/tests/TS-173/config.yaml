test_id: TS-173
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-142 deleted.json exclusion fix deployed on main
  - TS-136 tombstone persistence coverage
notes:
  - Reuses the TS-136 LocalTrackStateRepository delete fixture with the legacy deleted.json file intentionally absent.
  - Verifies deleteIssue still writes tombstone artifacts and keeps observable JQL search behavior correct when deleted.json does not physically exist.
