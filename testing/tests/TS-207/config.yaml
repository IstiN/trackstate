test_id: TS-207
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-141, TS-189]
notes:
  - Uses a ticket-specific Local Git fixture with configured Solution and Acceptance Criteria fields.
  - Verifies the JQL Search create flow completes as a clean Local Git create operation before checking the reopened form state.
  - Fails with git and UI snapshots when either the successful-save contract or the reset-state expectation regresses.
