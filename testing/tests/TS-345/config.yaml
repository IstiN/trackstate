test_id: TS-345
type: ui
framework: flutter_test
platform: widget
dependencies: []
notes:
  - Uses a long Summary and multi-paragraph Description to stress the layout while the Create issue form resizes from desktop to mobile widths.
  - Confirms the visible input fields and actions remain inside the rendered Create issue surface with no RenderFlex overflow exceptions.
