test_id: TS-748
type: ui
framework: flutter_test
platform: linux
dependencies:
  - TS-744
notes:
  - Reuses the production widget sync-refresh path covered by TS-742 to verify the selected JQL Search row exposes the expected selection styling tokens before and after refresh.
  - Verifies the selected row keeps the production-visible selected semantics flag, highlight background, selection border, emphasized key color, and emphasized summary weight across refresh.
  - Confirms an unselected sibling row keeps the non-selected styling tokens so the selected styling remains isolated to the active issue.
