test_id: TS-420
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Uses a ticket-scoped demo-backed repository fixture with project metadata and issue summaries ready while one visible issue keeps deferred detail loading.
  - Verifies the readiness contract snapshot directly, then confirms Dashboard and Settings remain reachable while JQL Search exposes a partial detail state instead of a blocked shell.
