test_id: TS-143
type: cli
framework: unittest
platform: python
dependencies:
  - Flutter SDK 3.35.3 available on PATH, at /tmp/flutter/bin/flutter, or via TRACKSTATE_FLUTTER_BIN / TS143_FLUTTER_BIN
notes:
  - Runs the live enforcement command `dart run tool/check_theme_tokens.dart lib` against the repository root.
  - Passes only when the command exits successfully, shows the success message, and does not report that the normalized `lib` directory target is missing.
