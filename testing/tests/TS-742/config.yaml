test_id: TS-742
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Launches the production TrackStateApp with a mutable in-memory repository that implements WorkspaceSyncRepository.
  - Seeds two Open issues, selects TRACK-742-B in JQL Search, mutates only that issue's description, then drives the production app-resume workspace sync refresh path.
  - Verifies the active JQL query stays populated, the selected issue remains selected, and the detail panel shows the refreshed description.
