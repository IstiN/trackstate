test_id: TS-41
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the local Git adapter blocks a save when issue main.md has dirty local edits.
  - Keeps the provider-backed description save assertion as the exact main.md write-path AC3 check.
  - Uses shared fixtures and app-screen abstractions instead of ticket-local framework wiring.
  - Establishes the same dirty main.md precondition before opening the live TrackState app.
  - Attempts the real issue-detail Edit/Save flow and expects visible commit/stash/clean guidance on the rendered tracker message surface.
