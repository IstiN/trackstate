test_id: TS-41
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the local Git adapter blocks a save when issue main.md has dirty local edits.
  - Keeps the provider-backed description save assertion as the exact main.md write-path AC3 check.
  - Uses a shared fixture instead of ticket-local framework wiring so the test stays within the standard testing layers.
  - The current TrackStateApp issue detail in this checkout is still read-only and does not expose an in-app Edit/Save path for this scenario.
