test_id: TS-41
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the local Git adapter blocks a save when issue main.md has dirty local edits.
  - Keeps the provider-backed description save assertion as a direct AC3 check.
  - Uses the real TrackStateApp UI to attempt the same issue-description edit plus Save flow and wait for visible commit/stash/clean guidance.
