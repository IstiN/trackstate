test_id: TS-1023
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Reuses the live startup retry recovery setup from TS-983 to force the workspace switcher into the recoverable Sync issue state.
  - Waits for the live retry request to succeed before opening the recovered workspace switcher.
  - Verifies both Add workspace and Save and switch are visible after recovery.
  - Confirms Save and switch starts disabled, then becomes enabled after selecting a different saved workspace row.
