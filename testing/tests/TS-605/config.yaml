test_id: TS-605
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Opens the deployed hosted TrackState app against the live setup repository in Chromium with a stored GitHub token.
  - Verifies the visible Project Settings > Repository access controls include Fine-grained token, Remember on this browser, and Connect token.
  - Passes only when Tab traversal on the live page moves from Fine-grained token to Remember on this browser and then to Connect token without skipping either control.
