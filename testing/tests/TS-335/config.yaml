test_id: TS-335
type: web
framework: playwright
platform: chrome
dependencies: []
notes:
  - Verifies the deployed hosted Create issue flow at a real 390x844 browser viewport.
  - Confirms the Create issue dialog fills the viewport from origin (0,0) with no side or bottom inset.
