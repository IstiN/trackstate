test_id: TS-667
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Verifies the saved-workspace deletion confirmation in the live Flutter Settings UI.
  - Verifies the production WorkspaceProfileService and TrackStateAuthStore remove the deleted workspace token and promote the remaining workspace to active state.
runtime_inputs:
  deleted_workspace_name: ts667-workspace-two
  fallback_workspace_name: ts667-workspace-one
  confirmation_title: Delete saved workspace
  confirmation_message: Delete ts667-workspace-two and remove its stored credentials? This action cannot be undone.
  active_label: Active
  run_command: flutter test testing/tests/TS-667/test_ts_667.dart --reporter expanded
