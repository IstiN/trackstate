test_id: TS-123
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-102]
notes:
  - Reuses the shared LocalTrackStateFixture and TrackStateAppComponent Local Git widget-test flow.
  - Verifies clean Local Git issue creation succeeds through the production-visible Create issue surface.
  - Confirms the create form closes, the new issue is visible in the UI, and Local Git records a dedicated create commit.
