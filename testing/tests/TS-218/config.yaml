test_id: TS-218
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-193 archive relocation failure keeps issue active
  - TS-201 archive relocation failure throws TrackStateRepositoryException
notes:
  - Reuses the TS-193 permission-failure fixture against a real local Git-backed repository.
  - Verifies the failed archive leaves git status, the staged index, and untracked files unchanged for TRACK-122.
