test_id: TS-404
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Uses a ticket-specific Local Git fixture with repository-backed statuses, workflows, issue types, and fields.
  - Verifies the Settings admin surface shows the required tabs, opens the status editor as a right-side drawer on wide layouts, and reopens it as a centered modal on narrow layouts.
  - Confirms the visible Save settings action uses TrackStateTheme primary/page tokens as the AC1 styling check.
