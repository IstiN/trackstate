test_id: TS-846
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token.
  - Resizes to a compact/mobile viewport and verifies the condensed workspace switcher trigger is present.
  - Uses real keyboard Tab navigation until the condensed trigger is focused, then presses Space.
  - Verifies the opened surface is a mobile sheet/full-screen overlay with the expected visible title and content.
