test_id: TS-281
type: api
framework: flutter_test
platform: linux
dependencies:
  - AC - Workflow and resolution enforcement deployed on main
notes:
  - Exercises the concrete `IssueMutationService.transitionIssue` path against a real Local Git-backed repository fixture.
  - Verifies the typed mutation result, persisted `main.md` frontmatter, and a fresh repository reload all clear `resolution` when a done issue is reopened to `to-do`.
