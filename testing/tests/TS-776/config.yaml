test_id: TS-776
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Reuses the hosted sync repository fixture from TS-773 to exercise the production TrackStateApp sync orchestration.
  - Verifies a hosted sync payload without load_snapshot_delta does not trigger loadSnapshot and leaves the visible issue detail unchanged.
