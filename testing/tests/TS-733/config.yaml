test_id: TS-733
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Launches the production TrackStateApp with a mutable in-memory repository that implements WorkspaceSyncRepository.
  - Seeds two Open issues, selects Issue-B in JQL Search, then applies a status change that closes Issue-B before triggering the production app-resume workspace sync refresh path.
  - Verifies the visible JQL query stays populated while the refreshed search results remove Issue-B and clear the visible issue selection.
