test_id: TS-1151
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Launches the production first-launch onboarding route with an empty SharedPreferences-backed workspace profile store.
  - Verifies the Local folder and Hosted repository chooser is visible before switching to the hosted path.
  - Verifies the hosted onboarding form renders the production Repository and Branch labels, the repository helper copy, and that Tab traversal reaches the hosted controls in logical order.
runtime_inputs:
  shared_preferences: {}
  run_command: flutter test testing/tests/TS-1151/test_ts_1151.dart --reporter expanded
