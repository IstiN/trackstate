test_id: TS-174
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-170 archiveIssue existing-issue path deployed on main
notes:
  - Exercises LocalTrackStateRepository.archiveIssue against a real local Git-backed fixture with an existing TRACK-122 issue.
  - Verifies the returned issue, repository index, committed markdown, and standard repository search all expose the archived lifecycle state after archiving.
