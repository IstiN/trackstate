test_id: TS-444
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Exercises the deployed hosted tracker startup path against https://istin.github.io/trackstate-setup/.
  - Blocks the deferred bootstrap tombstones artifact with a synthetic GitHub 403 rate-limit response and verifies the app shell still renders with Settings selected.

