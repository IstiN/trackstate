test_id: TS-152
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-148 archiveIssue lifecycle contract fix deployed on main
  - TS-135 active-to-archived repository regression coverage
notes:
  - Reuses the TS-135 LocalTrackStateRepository fixture in an initially archived state.
  - Verifies redundant archiveIssue calls stay idempotent: no exception, archived frontmatter preserved, repository metadata still archived, and standard repository search still exposes the archived issue.
