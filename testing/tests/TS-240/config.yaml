test_id: TS-240
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-226]
notes:
  - Reuses the TS-226 Local Git fixture that removes DEMO/config/fields.json from the repository fixture.
  - Verifies the user-visible fallback warning banner text is rendered before asserting the fallback Create issue form contents.
  - Confirms Local Git remains usable and the fallback Create issue form exposes the core Summary, Description, Save, and Cancel controls.
