test_id: TS-833
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token and two preloaded saved hosted workspaces.
  - Opens the desktop workspace switcher from Dashboard and observes the currently selected saved workspace.
  - Verifies Arrow Down keeps the panel open and moves the active selection from Hosted main workspace to Hosted alt workspace.
