test_id: TS-772
type: ui
framework: flutter_test
platform: widget
dependencies: []
notes:
  - Reuses the TS-734 mutable hosted repository fixture to drive the production workspace sync path.
  - Verifies a comments-only Issue-C refresh does not dispatch detail or other non-comments hydrations for the selected issue.
  - Confirms the visible Comments tab shows the synced comment content a user would read.
