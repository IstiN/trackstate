test_id: TS-666
type: api
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Seeds the production SharedPreferencesWorkspaceProfileService with an existing local workspace for `/user/projects/ts` on `main`.
  - Verifies creating the same target and default branch with writeBranch `feature-x` is rejected as a duplicate.
  - Verifies creating the same target on default branch `develop` still succeeds.
