test_id: TS-654
type: web
framework: flutter_test
platform: widget
dependencies: []
notes:
  - Pumps the production TrackState issue-detail flow with an in-memory repository whose `TRACK-12/links.json` contains a non-canonical `{"type":"blocks","target":"TRACK-11","direction":"inward"}` record.
  - Verifies the issue detail remains visible for the user and that captured system logs include a schema validation warning about the non-canonical link metadata.
