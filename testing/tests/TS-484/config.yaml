test_id: TS-484
type: cli
framework: python
platform: hosted-github
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - GH_TOKEN or GITHUB_TOKEN with write access to the hosted setup repository
  - Network access to GitHub APIs
notes:
  - Seeds a temporary hosted `TS-123` issue inside the live `DEMO` project.
  - Switches attachment storage to `github-releases` with the default `trackstate-attachments-` prefix.
  - Runs the real hosted CLI upload flow twice with the same `design_v1.png` filename.
  - Passes only when the release tag/title are deterministic, the public attachment identifier stays stable, and the second upload replaces the existing GitHub Release asset without creating duplicates.
