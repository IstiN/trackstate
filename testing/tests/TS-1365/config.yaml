test_id: TS-1365
type: install-script
framework: unittest
platform: linux
dependencies:
  - bash
  - curl
  - PowerShell Core (pwsh) — optional, for install.ps1 coverage
  - Python 3.12+
  - PyYAML
notes:
  - Verifies that the install scripts exit non-zero with a clear error when the GitHub API returns HTTP 403 during 'latest' release resolution.
  - Asserts that the error message identifies the API failure and suggests providing a pinned version URL to bypass the rate-limited resolution step.
runtime_inputs:
  install_script_path: scripts/install/install.sh
  optional_script_path: scripts/install/install.ps1
  platform: linux-x64
  binary_name: trackstate
  rate_limit_status: 403
