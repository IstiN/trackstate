test_id: TS-182
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the centralized Create issue overlay stays open while navigating from Dashboard to Board in Local Git mode.
  - Confirms the typed Summary value is preserved after the background view changes.
