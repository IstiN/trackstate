test_id: TS-816
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Reuses the dual-local signed-out workspace fixture from TS-809.
  - Verifies the inactive local row action is not only visible but opens the Connect GitHub authentication dialog.
runtime_inputs:
  active_workspace_kind: local
  inactive_workspace_kind: local
  active_workspace_auth_state: signed_out
  expected_inactive_state: visible local row that is not active
  expected_row_action: Connect GitHub
  expected_auth_flow: Connect GitHub dialog with token entry
  run_command: flutter test testing/tests/TS-816/test_ts_816.dart --reporter expanded
