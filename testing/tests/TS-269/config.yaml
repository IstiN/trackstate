test_id: TS-269
type: cli
framework: unittest
platform: python
dependencies:
  - Dart SDK available on PATH or via TRACKSTATE_DART_BIN
  - git CLI available on PATH for seeding a disposable Local Git repository
notes:
  - Executes `trackstate --target local` from inside a disposable Local Git repository without `--path`.
  - Verifies the CLI resolves the local target to the current working directory.
