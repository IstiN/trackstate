test_id: TS-615
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Opens the deployed hosted TrackState web app and connects a real GitHub session until the desktop header exposes the `Attachments limited` repository access state.
  - Measures the live search field, Create issue button, and `Attachments limited` control from the rendered desktop top bar without modifying production code.
