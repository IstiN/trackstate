test_id: TS-615
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Opens the deployed hosted TrackState web app and reaches a real GitHub session until the desktop workspace switcher exposes the `Attachments limited` repository access state.
  - Verifies the documented 32px parity rule by comparing the live `Search issues` field baseline with the rendered `Create issue` and repository access controls without modifying production code.
