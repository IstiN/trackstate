test_id: TS-615
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Opens the deployed hosted TrackState web app and connects a real GitHub session until the desktop header exposes the `Attachments limited` repository access state.
  - Verifies the documented 32px parity rule by comparing the live `Search issues` input baseline with the rendered `Create issue` and `Attachments limited` controls without modifying production code.
