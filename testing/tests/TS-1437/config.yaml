test_id: TS-1437
type: web
framework: playwright
platform: chromium
dependencies: []
notes:
  - Opens the deployed hosted TrackState app in a browser-authenticated session and navigates to Project Settings.
  - Clicks the Connect GitHub button and verifies the connection dialog opens.
  - Passes only when the hosted button is clickable within the timeout and the Fine-grained token dialog renders.