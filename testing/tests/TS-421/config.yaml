test_id: TS-421
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Exercises the deployed TrackState hosted demo.
  - Verifies comments and history hydration stay deferred until the user activates each tab.
