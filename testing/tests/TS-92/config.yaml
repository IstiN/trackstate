test_id: TS-92
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Verifies the local Git adapter blocks a save when issue main.md has staged local edits.
  - Reuses the shared dirty-local save component and TrackState app screen abstractions.
  - Stages DEMO/DEMO-1/main.md before attempting the same description edit/save flow in the live app.
  - Confirms the user-visible error guidance includes commit, stash, and clean recovery instructions.
