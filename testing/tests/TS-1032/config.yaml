test_id: TS-1032
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app against the live implementation.
  - Delays the GitHub /user startup probe by 5 seconds to make the pending phase observable.
  - Runs the TS-1032 startup state-machine validator utility against live startup samples.
  - Confirms the observed startup path reaches bootstrap, pending, and resolved before the visible shell becomes interactive.
