test_id: TS-487
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Forces the viewer locale to de before launching the production Local Git app.
  - Clears the de and en status translations from Settings > Locales and verifies the visible JQL Search issue row falls back to the canonical status name.
  - Reuses the shared locale-resolution repository fixture together with TrackStateAppScreen and SettingsScreenRobot.
