test_id: TS-112
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-41, TS-93]
notes:
  - Verifies the dirty-save error banner stays visible for 10 seconds without user interaction.
  - Confirms the same visible banner can then be dismissed manually through the user-facing close control.
