test_id: TS-850
type: web
framework: playwright
platform: chromium
dependencies:
  - Playwright for Python with Chromium installed
  - GH_TOKEN or GITHUB_TOKEN with access to IstiN/trackstate-setup
notes:
  - Opens the deployed TrackState web app with a stored hosted token.
  - Locates the desktop workspace switcher trigger on Dashboard.
  - Verifies the trigger aria-expanded value is false before mouse activation, true after opening with a click, and false again after the second click closes the surface.
