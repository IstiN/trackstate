test_id: TS-378
type: cli
framework: unittest
platform: python
dependencies:
  - Flutter available on PATH for the compiled CLI harness
notes:
  - Verifies the exact ticket command `trackstate read account-by-email user@example.com` returns the documented unsupported CLI error contract.
  - Runs the exact ticket command from a seeded Local Git repository so the assertion is not dominated by detached-HEAD checkout state.
  - Uses a temporary executable compiled from the current checkout so the probe preserves the ticket command shape while running from the seeded repository.
