test_id: TS-799
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
  - git available on PATH
notes:
  - Launches the production first-launch local-folder onboarding flow with an actual empty temp directory.
  - Verifies the selected folder is inspected by the real onboarding service and is classified as ready to initialize.
  - Confirms the visible Initialize action is enabled and that activating it creates a real local TrackState scaffold and opens the initialized workspace.
runtime_inputs:
  picker_mode: initialize-folder
  folder_fixture: empty temp directory with no files or hidden metadata
  run_command: flutter test testing/tests/TS-799/test_ts_799.dart --reporter expanded
