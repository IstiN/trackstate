test_id: TS-809
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Uses the shared TrackStateAppComponent workspace-switcher abstraction.
  - Seeds two valid local workspaces so one inactive local row is visible while signed out.
  - Verifies the inactive local workspace row visibly renders Connect GitHub without expanding the scope into the auth-dialog flow.
runtime_inputs:
  active_workspace_kind: local
  inactive_workspace_kind: local
  active_workspace_auth_state: signed_out
  expected_inactive_state: visible local row that is not active
  expected_row_action: Connect GitHub
  run_command: flutter test testing/tests/TS-809/test_ts_809.dart --reporter expanded
