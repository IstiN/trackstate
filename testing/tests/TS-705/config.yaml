test_id: TS-705
type: web
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Launches the production onboarding screen in a WidgetTester harness with empty SharedPreferences to reproduce the first-launch experience.
  - Verifies visible copy, interactive semantics labels, forward and backward keyboard traversal for both local and hosted onboarding forms, and WCAG contrast thresholds on the rendered text surfaces.
  - Runs `dart run tool/check_theme_tokens.dart lib/ui/features/tracker/views/trackstate_app.dart` to confirm the onboarding implementation stays within centralized theme tokens.
