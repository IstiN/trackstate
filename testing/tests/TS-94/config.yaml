test_id: TS-94
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Reproduces dirty-local-files issue creation from the live Local Git app flow.
  - Attempts the full create flow and verifies visible commit/stash/clean guidance after submission.
  - Fails with a user-visible UI snapshot when the create-issue capability is unavailable or incomplete.
