test_id: TS-151
type: ui
framework: flutter_test
platform: linux
dependencies: [TS-141]
notes:
  - Reuses the Local Git custom-fields fixture from TS-141 so the production-visible Create issue form renders Solution, Acceptance Criteria, and Diagrams.
  - Verifies leaving those optional custom fields empty still allows successful creation, user-visible discovery of the created issue, and filesystem persistence of core Summary and Description data without null placeholders.
