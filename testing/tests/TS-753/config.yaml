test_id: TS-753
type: ui
framework: flutter_test
platform: widget
dependencies:
  - Flutter SDK available on PATH
notes:
  - Reuses the mutable hosted provider-backed repository flow from TS-732 so the production JQL Search, issue-detail, and workspace-sync refresh logic runs unchanged.
  - Removes the currently selected issue during a background sync, waits for the live issue-unavailable banner, and verifies the user can dismiss that banner from the visible UI.
runtime_inputs:
  issue_key: TRACK-12
  remaining_issue_key: TRACK-11
  expected_message_fragment: no longer available
  run_command: flutter test testing/tests/TS-753/test_ts_753.dart --reporter expanded
