test_id: TS-623
type: api
framework: flutter_test
platform: linux
dependencies:
  - Local Git-backed repository writes enabled through the production LocalTrackStateRepository provider
notes:
  - Reuses the shared blocked-delete repository fixture to verify a parent issue cannot be deleted while an active child issue still exists.
  - Confirms the mutation returns the exact validation failure, keeps the hierarchy searchable, and leaves repository state unchanged.
