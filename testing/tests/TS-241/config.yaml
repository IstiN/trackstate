test_id: TS-241
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-223 Concurrent issue deletes lose tombstone/index updates and break repository reload
notes:
  - Reuses the TS-215 concurrent-delete fixture with three delete targets and one surviving issue.
  - Triggers concurrent LocalTrackStateRepository.deleteIssue calls through the repository service and verifies tombstones.json keeps all deleted issue keys.
