test_id: TS-598
type: api
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Exercises the production IssueMutationService updateFields path against a seeded local Git repository where the requested issue key is absent from the repository index.
  - Verifies the typed mutation result reports a machine-readable not-found classification and leaves the repository state unchanged for integrated clients.
