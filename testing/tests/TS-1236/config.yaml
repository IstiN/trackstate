test_id: TS-1236
type: ui
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Opens the production issue-detail Comments tab through the reusable issue-detail accessibility fixture.
  - Verifies the empty comment composer renders the exact placeholder text "Add a comment..." for TRACK-12.
  - Confirms the placeholder is visibly rendered inside the input before any user text is entered.
