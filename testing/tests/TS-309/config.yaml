test_id: TS-309
type: api
framework: flutter_test
platform: linux
dependencies: []
notes:
  - Seeds a disposable Local Git repository where PROJECT-1 history is represented only by Git commits.
  - Verifies the repository API normalizes creation, description update, and status transition events without any history.md sidecar.
  - Confirms the observable API payload an integrated client/UI would consume contains the expected business events and metadata.
