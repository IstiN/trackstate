test_id: TS-622
type: api
framework: flutter_test
platform: linux
dependencies:
  - AC - Reopening an issue from Done to To Do clears resolution on main
notes:
  - Reuses the concrete Local Git-backed issue transition flow already covered by the shared reopen fixture.
  - Verifies the returned mutation payload, freshly reloaded repository state, and persisted main.md frontmatter all clear resolution when reopening a done issue.
  - Human-style verification checks the issue remains visible with the expected summary and To Do status label while no resolution is exposed.
