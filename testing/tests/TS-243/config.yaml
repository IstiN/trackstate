test_id: TS-243
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-223 Concurrent issue deletes lose tombstone/index updates and break repository reload
notes:
  - Uses the Ts215ConcurrentLegacyDeletedIndexFixture with focus on repository reload and search functionality
  - Verifies that repository reload does not fail with Git 'path does not exist' errors after concurrent deletes
  - Confirms that search queries remain functional and return only surviving issues
