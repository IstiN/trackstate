test_id: TS-154
type: api
framework: flutter_test
platform: linux
dependencies:
  - TS-148 archiveIssue lifecycle contract fix deployed on main
  - TS-135 archive lifecycle regression coverage
notes:
  - Verifies LocalTrackStateRepository exposes the full TrackStateRepository contract.
  - Exercises archiveIssue through a dynamic invocation path matching the TS-148 failure mode.
