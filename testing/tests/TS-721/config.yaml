test_id: TS-721
type: ui
framework: flutter_test
platform: linux
dependencies:
  - Flutter SDK available on PATH
notes:
  - Verifies first-run local onboarding saves a workspace profile for a valid local repository.
  - Verifies the new local workspace becomes active and the app routes directly into the Tracker UI.
  - Verifies the Tracker loads the selected local repository context with the Local Git access state.
runtime_inputs:
  selected_workspace_branch: main
  expected_access_label: Local Git
  expected_issue_summary: Local identity issue
  run_command: flutter test testing/tests/TS-721/test_ts_721.dart --reporter expanded
